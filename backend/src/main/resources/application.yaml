server:
  port: 8080

spring:
  application:
    name: review-api
  jackson:
    serialization:
      write-dates-as-timestamps: false

logging:
  level:
    root: WARN
    com.gropyus.reviewboard: INFO
    org.springframework.web: INFO
